name: Close PRs from Forks that didn't run CI

on:
  pull_request_target:
    types: [opened, reopened]
    branches:
      - main

jobs:
  verify:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
      contents: read
    steps:
      - uses: NotTodayThankyou/NotTodayThankyou@JS_Action
        with:
          allowed-authors: "JamesParrott-machine-account"
          max-prs-per-day: "0"
          require-associated-issue: "true"
