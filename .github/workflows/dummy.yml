name: Hello World
description: 'A dummy workflow to test if incoming PRs ran the workflows on their fork'

on:
  push:
  workflow_dispatch:
  
jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello, World!"
